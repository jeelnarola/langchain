[project]
name = "mcp-hub"
version = "0.1.0"
description = "MCP Server Hub with Telegram integration"
requires-python = ">=3.8"
dependencies = [
    "mcp>=1.0.0",
    "python-telegram-bot>=20.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
dev-dependencies = []