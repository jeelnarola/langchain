[project]
name = "mcp-hub"
version = "0.1.0"
description = "MCP Server Hub with Telegram integration"
requires-python = ">=3.13"
dependencies = [
    "mcp>=1.18.0",
    "python-telegram-bot>=20.0",
]

[tool.uv]
dev-dependencies = []
